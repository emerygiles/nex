fastapi==0.115.6
uvicorn[standard]==0.34.0
httpx==0.28.1
pydantic==2.10.4
pydantic-settings==2.7.1
python-dotenv==1.0.1
sse-starlette==2.2.1
# Optional dev-time AI fallback (only needed if AI_PROVIDER=anthropic)
anthropic==0.42.0
# Optional build-time only: regenerate the ATT&CK telemetry map (scripts/build_attack_mapping.py).
# Runtime loads the committed backend/data/attack_stix_mapping.json and does NOT need this.
# mitreattack-python>=4.0.0
